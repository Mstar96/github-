私有IP范围
10.0.0.0 - 10.255.255.255
172.16.0.0 - 172.31.255.255
192.168.0.0 - 192.168.255.255

只有 WPA-PSK（预共享密钥）模式支持使用 TKIP 进行数据加密。如果使用 WPA2 或 WPA2-PSK，AES 加密。
路由表-直连路由优先级为0

802.11a和802.11ac主要支持5GHz频段，而802.11b和802.11g仅支持2.4GHz频段。802.11n和802.11ax则支持双频段，提供更广泛的应用场景和更好的性能。

rule 5 permit source 192.168.1.2 0：
规则 5 允许源 IP 地址为 192.168.1.2 的流量通过。
source 192.168.1.2 0 表示完全匹配 192.168.1.2 这个 IP 地址。0 是 反掩码（wildcard mask），与子网掩码相对，0 表示完全匹配源地址中的所有位。


STP协议
1.在 STP（Spanning Tree Protocol）协议中，根交换机（Root Bridge） 是通过交换机的 优先级（Priority） 和 MAC 地址 来选举的。假设所有交换机的优先级设置相同，那么 根交换机 将根据 MAC 地址 来决定，MAC 地址较小的交换机将成为 根交换机。
2.端口处于forwarding状态时，端口既转发用户流量叶处理BPDU报文
3.当 STP 网络中的拓扑发生变化，例如某个端口的状态发生改变（比如一个端口从转发状态变为阻塞状态，或者发生了链路故障），源交换机 会通过 发送 TCN BPDU 来通知网络中的其他交换机。
4.任何端口都可以直接转换为disabled状态
5.端口在learning状态下需要等待转发延时之后 才能转化为forwarding状态
6.收到RSTP的配置BPDU时会丢弃
四种状态：
Disabled（禁用状态）：端口被禁用，不参与STP运算。
Blocking（阻塞状态）：端口处于阻塞状态，不转发数据帧，但可以接收BPDUs（桥协议数据单元）。
Listening（侦听状态）：端口处于侦听状态，不转发数据帧，但可以接收和处理BPDUs，准备进入学习状态。
Learning（学习状态）：端口处于学习状态，不转发数据帧，但可以接收BPDUs，并开始学习MAC地址。
Forwarding（转发状态）：端口处于转发状态，可以正常转发数据帧。


ppp协议从不可用转到可用是通过LCP协议进行链路参数的协商
LCP协议支持协商认证、检测链路环路、最大接受单元MTU
链路配置阶段（LCP）：协商链路参数并建立链路。
认证阶段：可选，使用PAP或CHAP进行身份验证。
网络协议协商阶段（NCP）：协商和配置上层协议（如IP、IPX等）。
链路维护阶段：定期检查链路的可用性。
数据传输阶段：一旦链路建立和维护完成，开始进行数据传输。


RSTP协议（园区规划可以避免出现二层环路）
三个状态（丢弃、学习和转发）

OSPF协议（网络层对应的协议号89，优先级10）-基于IP协议
在广播类型和NBMA类型才需要选举DR和BDR
在ExStart状态下通过Route ID确认DD报文主从关系
LSR报文请求本地缺少的LSA
用DD报文描述自己的LSDB
FULL状态表示邻接关系已经建立
用LSU报文向邻居发送LSA
用HELLO报文发现和维护邻居关系
使用LSACK对接收到的LSU报文进行确认
五种报文：HELLO、DD（数据库描述）、LSR（链路状态请求）、LSU（链路状态更新）、LSAck（链路状态确认），其中请求的、描述的就是路由器所拥有的链路状态报告LSA
邻居状态机共有8种状态，分别是：
Down、Attempt、Init、2-way、Exstart、Exchange、Loading、Full。
在这些状态中，稳定状态包括Down、2-way和Full状态。
通常，OSPF会选择路由器上最高的IP地址作为Router ID。
如果路由器上所有的IP地址都相同，那么它会选择最高的环回接口IP地址（LoopBack interface）。



OSI参考模型-有哪些层，每层有什么设备
1.物理层：仅关心如何通过物理媒介传输数据
    设备：
    集线器（Hub）：一种简单的多端口设备，将一个端口接收到的数据复制到其他端口，通常没有智能，工作在物理层。
    网卡（NIC，Network Interface Card）：计算机或设备用于连接网络的硬件，工作在物理层，负责数据的发送和接收。
    交换机的物理端口：尽管交换机工作在数据链路层，但它的端口与物理层相关，负责物理连接。
    电缆（铜缆、光纤等）：用于传输比特流的物理媒介。
    常用协议：
    IEEE 802.3（以太网）
    IEEE 802.11（Wi-Fi）
    USB（通用串行总线）
    蓝牙协议
2.数据链路层：负责数据帧的封装和传输，确保在物理媒介上可传输，负责物理地址寻址和流量控制
    设备：
    交换机（Switch）：工作在数据链路层，负责通过 MAC 地址转发数据帧，实现局域网内的流量管理。
    网桥（Bridge）：连接不同的网络段，工作在数据链路层，根据 MAC 地址进行数据帧转发。
    常用协议:
    Ethernet（以太网）
    PPP（点对点协议）
    STP（生成树协议）
    VLAN（虚拟局域网协议）
3.网络层：负责数据包的 寻址和路由选择，确定数据如何从源设备传输到目标设备。它处理 IP地址，并通过 路由器 实现不同网络之间的互联
    设备：
    路由器（Router）：工作在网络层，使用 IP 地址来决定如何转发数据包，跨越不同的子网或网络。
    常用协议：
    IP（互联网协议）
    ICMP（网络故障报告）
    ARP（地址解析协议）
    RIP/OSPF/BGP（路由协议）
4.传输层:确保数据传输的 可靠性，提供 端到端的通信服务。它通过 端口号 将数据传递给正确的应用程序。主要协议包括 TCP（提供可靠的连接）和 UDP（提供无连接的通信）。
传输层负责分段数据流量、流量控制和错误校验。
    设备:
    传输层不直接依赖硬件设备，但应用程序通过操作系统的传输协议栈来实现这一层的功能。
    防火墙 和 负载均衡器：虽然这些设备主要工作在不同的层次上，但它们也参与处理传输层的流量
    常用协议:
    TCP（面向连接，可靠传输）
    UDP（无连接，快速传输）
5.会话层:负责 建立、管理和终止会话，即两个通信系统之间的对话。它确保数据的 同步 和 恢复，以及数据交换过程中的 流控制。
会话层还负责 双向通信 的管理，确保数据的顺序和会话状态。
    设备:
    会话层通常是 软件层，没有直接的硬件设备。常见的会话层协议有 NetBIOS、RPC（远程过程调用）等。
    常用协议:
    RPC（远程过程调用）
    NetBIOS（网络基本输入输出系统）
    PPTP（点对点隧道协议）
6.表示层:负责数据的 格式化 和 编码，确保发送方和接收方能够理解彼此交换的数据。例如，它负责数据的 加密、解密 和 压缩。
    设备：
    表示层一般是由 软件协议 来处理，通常没有独立的硬件设备。它负责将应用层的数据转换为可传输的格式。
    常用协议:
    SSL/TLS（安全传输）
    JPEG/GIF/PNG（图片格式）
    MPEG/MP3（多媒体格式）
7.应用层：提供了最终用户的服务和应用程序接口，负责与用户的交互。应用层协议定义了各种网络服务的标准
    设备：应用层通常没有专门的硬件设备，主要依赖于 应用程序 和 软件 来完成。常见的应用层服务包括 Web 浏览器、电子邮件客户端、FTP 客户端等。
    常用协议:
    HTTP/HTTPS（网页浏览）
    SMTP/IMAP/POP3（邮件传输）
    FTP（文件传输）
    DNS（域名解析）
    Telnet/SSH（远程登录）
    SNMP（网络管理）
```
应用层       [ 应用数据 ]
  ↓
传输层       [ 传输头 | 应用数据 ]
  ↓
网络层       [ 网络头 | 传输头 | 应用数据 ]
  ↓
数据链路层   [ 链路头 | 网络头 | 传输头 | 应用数据 | 链路尾 ]
  ↓
物理层       [ 101010101... ]
```

MPLS网络:
多协议标签交换，Multiprotocol Label Switching）是一种在IP骨干网中使用的高速网络交换标准
MPLS的核心在于利用标签（label）进行数据转发，通过在数据包上附加一个标签来实现快速的路径选择和数据包转发。
标签头封装在数据链路头和网络头之间
帧模式封装：
帧模式封装是直接在报文的二层头部和三层头部之间增加一个MPLS标签头。这种封装方式适用于以太网（Ethernet）和点对点协议（PPP）等帧模式网络。
MPLS标签封装在链路层和网络层之间，使得标签能够被任意的链路层所支持。
信元模式封装：
信元模式封装主要应用于ATM（异步传输模式）网络，这种封装方式将MPLS标签作为信元的一部分进行传输。
LDP（Label Distribution Protocol，标签分发协议）是MPLS（多协议标签交换）网络中的一种关键控制协议。
LDP的主要功能包括：
使用Hello消息进行LSR发现，建立和维护LDP会话。
标签映射的通告，即一个LSR可以通知另一个LSR其已经形成的标签捆绑。
使用Notification消息进行协议管理，以处理错误和状态通知。

BFD（Bidirectional Forwarding Detection，双向转发检测）是一种用于快速检测网络设备之间双向路径是否存在故障的网络协议
与OSPF、IS-IS、BGP、VRRP、静态路由等路由协议联动使用
控制报文是UDP报文，端口号为3784

LACP模式（Link Aggregation Control Protocol，链路聚合控制协议）：网络设备之间通过发送LACPDU来交互信息，自动形成聚合链路并启动聚合链路收发数据
活动端口（Active ports）：这些端口参与数据的转发，并且可以发送和接收LACPDU来维护链路聚合的状态。
非活动端口（Passive ports）：这些端口不参与数据转发，但仍然可以发送和接收LACPDU。它们通常用于备份，以防活动端口发生故障。
选择端口（Selected ports）：这些端口被选中用于链路聚合，它们可以是活动端口也可以是非活动端口。
非选择端口（Unselected ports）：这些端口没有被选中用于链路聚合，因此不参与数据转发。

以太网数据帧中,Length\Type字段<=1500时候，代表数据字段长度（MTU是1500字节），大于1536（0X0600）时表示封装的上层协议类型
类型/长度值	含义
0x0800	IPv4
0x0806	ARP
0x86DD	IPv6
0x8100	VLAN Tag (802.1Q)
0x8847	MPLS
0x8848	MPLS (Extended)
0x880B	PPP

以太数据帧格式：
| 前导码 | 帧头 | 目的MAC地址 | 源MAC地址 | 类型/长度 | 数据 | FCS |
前导码（Preamble）: 7 字节，帮助同步传输。
帧头（Frame Header）: 包括目的 MAC 地址（6 字节）和源 MAC 地址（6 字节）。
类型/长度（Type/Length）: 2 字节，表示上层协议类型或数据部分的长度。
数据（Data）: 这个字段携带具体的有效负载数据。
FCS（Frame Check Sequence）: 4 字节，CRC 校验码，用于数据完整性检测。

在以太网交换机中，Trunk、Access和Hybrid端口是三种不同的端口模式，它们对数据帧的处理方式各有特点：
Trunk端口：
主要用于交换机之间的连接。
允许多个VLAN的数据帧通过。
发送和接收的数据帧通常带有VLAN标签（tagged）。
根据允许通过的VLAN列表来处理数据帧。如果数据帧的VLAN ID在允许列表中，且与端口的PVID（Port VLAN ID）相同，则剥离标签后转发；如果不同，则保留标签转发。如果不在允许列表中，则丢弃该帧
。
Access端口：
主要用于连接终端设备，如计算机、IP电话等。
只能属于一个VLAN，发送和接收的数据帧不带VLAN标签（untagged）。
当数据帧进入Access端口时，交换机会根据该端口的PVID为数据帧打上相应的VLAN标签。发送时，如果数据帧的VLAN ID与端口的PVID相同，则剥离标签后发送；如果不同，则直接丢弃该帧
。
Hybrid端口：
既可以连接终端设备，也可以连接其他交换机。
允许多个VLAN的数据帧通过，并且可以根据配置决定是否为某些VLAN的数据帧打上或去掉VLAN标签。
它结合了Access和Trunk端口的功能，可以灵活地处理不同类型的链路。在接收数据帧时，如果数据帧没有标签，则打上端口的PVID；如果有标签，则检查该VLAN是否允许通过。在发送数据帧时，根据VLAN在本接口的属性（Tagged或Untagged），决定是否剥离VLAN标签


AR路由器
AC
AP
SNMP协议
QoS拥塞避免技术
